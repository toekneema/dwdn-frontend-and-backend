# dwdn-frontend-and-backend

Frontend code is in the `client` folder. Backend smart contract code is in `smart_contract`.

To run the site locally, look at the instructions in the client/README.md file.
